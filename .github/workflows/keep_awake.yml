name: Hyper Guardian Keep-Awake Protocol

on:
  schedule:
    # 5분마다 한 번씩 앱을 깨웁니다 (최대 빈도 설정)
    - cron: '*/5 * * * *'
  workflow_dispatch: # 수동으로도 가동 가능하게 설정

jobs:
  ping_app:
    runs-on: ubuntu-latest
    steps:
      - name: Send Deep Survival Signal to Hyper Guardian (v13.6)
        run: |
          # Simplified ping to avoid redirect loops and handle auth-wall gracefully
          curl -I --max-time 10 https://hyper-etf-guardian-kvkaanb3rkxwcisrmhxvn2.streamlit.app/
